import React from 'react';

function Cart() {
  return <h1 style={{ textAlign: "center", marginTop: "50px" }}>Cart is empty</h1>;
}

export default Cart;
/*src/pages/Cart.js
Purpose: Represents the Cart Page.

Usage: Initially displays a simple "Cart is empty" message. Later, it will integrate with localStorage or backend for managing cart items.*/